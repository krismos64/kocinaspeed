{% extends 'base.html.twig' %}

{% block title %}Contact Us{% endblock %}

{% block body %}
    <h1>Contact Us</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Send</button>
    {{ form_end(form) }}
{% endblock %}